# Simulation outputs
sim/
tb/sim_build/
tb/results.xml
tb/__pycache__/
*.vcd
*.fst
*.lxt

# Quartus (school)
db/
incremental_db/
output_files/
*.qpf
*.qsf
*.sof
*.pof

# Yosys
*.blif
*.json

# OS
.DS_Store

# Personal / job-search notes (never commit — these contain apply status + visa notes)
docs/job_search*.md
docs/apply_tracker*.md

# Local project notes (Claude collaboration notes, not for public repo)
CLAUDE.md
docs/roadmap.md

# Stray PDFs at repo root (lab reports, etc. — put real docs in docs/ intentionally)
/*.pdf

# Secrets (defensive)
.env
*.key
*.pem
credentials.json
secrets.*
